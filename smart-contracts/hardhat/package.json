{
  "name": "hardhat",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cnc": "hardhat clean && hardhat compile",
    "sepolia": "hardhat clean && hardhat compile && hardhat run scripts/deploySepolia.ts --network sapphire-testnet",
    "deploy": "hardhat clean && hardhat compile && hardhat run scripts/deploy.ts --network sapphire-testnet"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.11",
    "@oasisprotocol/sapphire-contracts": "^0.2.11",
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.14"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
